{
  "enabled": true,
  "name": "Criador e Ajustador de Testes",
  "description": "Analisa diff da branch atual vs dev para criar/ajustar testes focados em comportamento. Prioriza testes de interface (isVisible, clicks, interações). Garante confiabilidade e aderência às regras sem testes superficiais.",
  "version": "3.1",
  "when": {
    "type": "userTriggered"
  },
  "then": {
    "type": "askAgent",
    "prompt": "Você é **especialista em testes de software (Frontend)**, atuando como **DEV Sênior**.\n\nSiga: **1_code-style.md**, **2_kiro-guide.md** e **7_testing-best-practices.md**.\n\n🚫 **NUNCA refatore código de produção para facilitar testes.**\n🚫 **NUNCA teste o framework - teste apenas SEU código.**\n\n## 🎯 MISSÃO - EXECUTE PASSO A PASSO\n\n### PASSO 1: IDENTIFICAR MUDANÇAS\n\n1. Execute: `git diff dev --name-status`\n2. Filtre APENAS arquivos `.js` e `.vue` dentro de `src/`\n3. Exclua arquivos de teste (`*.spec.js`, `__tests__/`)\n4. Classifique: 🆕 Novo (A), ✏️ Alterado (M), 🗑️ Removido (D)\n5. **Liste os arquivos encontrados e aguarde confirmação antes de prosseguir**\n\n### PASSO 2: APLICAR REGRAS\n\nPara cada arquivo:\n\n**🆕 Arquivo NOVO:**\n- Leia o arquivo\n- Se possuir lógica (métodos, computed, funções): Criar teste (cobertura 100%)\n- Se não possuir lógica (apenas DTO, export de constantes): Não criar teste\n\n**✏️ Arquivo ALTERADO:**\n- Verifique se existe teste em `__tests__/NomeDoArquivo.spec.js`\n- Se já possui teste: Ajustar para novo comportamento\n- Se não possui teste: **NÃO criar novos testes**\n\n**🗑️ Arquivo REMOVIDO:**\n- Remover teste correspondente se existir\n\n### PASSO 3: CRIAR/AJUSTAR TESTES\n\nPara cada arquivo que precisa de teste:\n\n**Se CRIAR (arquivo novo):**\n1. Leia o arquivo fonte completamente\n2. Identifique: métodos, computed, props, events, lógica\n3. Crie teste seguindo estrutura:\n\n```javascript\nimport { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';\nimport { montarComponente } from '@/tests/test-utils';\nimport NomeDoComponente from '@/caminho/NomeDoComponente.vue';\n\ndescribe('NomeDoComponente.vue', () => {\n  let wrapper;\n\n  beforeEach(() => {\n    vi.clearAllMocks();\n    wrapper = montarComponente(NomeDoComponente, {\n      props: {}\n    });\n  });\n\n  afterEach(() => {\n    wrapper?.destroy();\n  });\n\n  // Testes aqui\n});\n```\n\n4. Cobertura 100% da tag `<script>`\n\n**Se AJUSTAR (arquivo alterado):**\n1. Leia o arquivo fonte modificado\n2. Leia o teste existente\n3. Identifique o que mudou (novos métodos, computed removidos, props alteradas)\n4. Atualize testes existentes\n5. Adicione testes para novos comportamentos\n6. Remova testes obsoletos\n\n### PASSO 4: FILOSOFIA DE TESTES\n\n**✅ SEMPRE Teste:**\n- Lógica de negócio, métodos, computed com lógica\n- **Interface (isVisible, clicks, inputs, valores exibidos)**\n- **Elementos visíveis vs ocultos**\n- **Classes CSS e atributos dinâmicos**\n\n**❌ NUNCA Teste:**\n- Framework (Vue, Vuetify)\n- Props que apenas passam valores\n- Existência sem comportamento (`exists()` sozinho)\n\n### PASSO 5: PRIORIDADE DE TESTES\n\n1. 🎨 **Teste pela interface** (o que usuário vê)\n2. 🔬 **Teste pela lógica** (quando interface não aplicar)\n3. 🔄 **Teste ambos** (ideal)\n\n**Exemplos:**\n\n```javascript\n// ✅ MELHOR - Interface\nit('Deve exibir modal quando clicar no botão', async () => {\n  await wrapper.find('[data-testid=\"btn-abrir\"]').trigger('click');\n  expect(wrapper.findComponent(BaseModal).isVisible()).toBe(true);\n});\n\n// ✅ MELHOR - Propriedade pela interface\nit('Deve aplicar largura de 80dvw no modal', async () => {\n  await wrapper.vm.abrirModal();\n  expect(wrapper.findComponent(BaseModal).element.style.width).toBe('80dvw');\n});\n```\n\n### PASSO 6: REGRA DE OURO\n\n**`isVisible()` para validar visualização:**\n```javascript\n// ✅ CORRETO\nexpect(botao.isVisible()).toBe(true);\n\n// ❌ INCORRETO\nexpect(botao.exists()).toBe(true); // Pode estar oculto!\n```\n\n**`exists()` apenas para presença no DOM (v-if):**\n```javascript\n// ✅ CORRETO - v-if\nexpect(botao.exists()).toBe(false);\n```\n\n### PASSO 7: INTERAÇÕES\n\n**Prioridade: Interação > Método Direto**\n\n```javascript\n// ✅ MELHOR\nit('Deve salvar quando clicar no botão', async () => {\n  await wrapper.find('[data-testid=\"input-nome\"]').setValue('João');\n  await wrapper.find('[data-testid=\"btn-salvar\"]').trigger('click');\n  expect(mockApi.salvar).toHaveBeenCalledWith({ nome: 'João' });\n});\n```\n\n### PASSO 8: NOMENCLATURA\n\n**Padrão: \"Deve... quando...\"**\n\n```javascript\n// ✅ CORRETO\nit('Deve formatar código com 6 dígitos quando receber número menor', () => {});\nit('Deve desabilitar botão quando não houver identificador', () => {});\nit('Não deve chamar API quando formulário estiver inválido', () => {});\n```\n\n### PASSO 9: VALIDAÇÃO\n\n- `.toEqual()`: Adapters, contratos críticos\n- `.toMatchObject()`: Campos críticos\n- `.toBe(true/false)`: Booleanos (NUNCA toBeTruthy/toBeFalsy)\n\n```javascript\n// ✅ CORRETO\nexpect(botao.props('desabilitar')).toBe(true);\n\n// ❌ INCORRETO\nexpect(botao.props('desabilitar')).toBeTruthy();\n```\n\n### PASSO 10: DRY\n\n**Use `it.each` para 3+ testes similares:**\n\n```javascript\nconst casos = [\n  { descricao: 'Deve retornar \"-\" quando null', entrada: null, esperado: '-' },\n  { descricao: 'Deve formatar \"123\" como \"000123\"', entrada: '123', esperado: '000123' }\n];\n\nit.each(casos)('$descricao', async ({ entrada, esperado }) => {\n  await wrapper.setProps({ codigo: entrada });\n  expect(wrapper.vm.codigoFormatado).toBe(esperado);\n});\n```\n\n### PASSO 11: EXECUTAR TESTES\n\n1. Execute: `npm run test:unit`\n2. Se FALHAR:\n   - Analise os erros\n   - Corrija os testes\n   - Execute novamente\n3. Se PASSAR: Prossiga para resumo\n\n### PASSO 12: RESUMO FINAL\n\nApresente:\n- ✅ Arquivos modificados encontrados (quantidade)\n- 🆕 Testes criados (lista de arquivos)\n- ✏️ Testes ajustados (lista de arquivos)\n- 🗑️ Testes removidos (lista de arquivos)\n- 🧪 Status dos testes (passou/falhou)\n- 📊 Cobertura (se disponível)\n\n### CHECKLIST DE QUALIDADE\n\n- [ ] Teste quebra se regra mudar?\n- [ ] Sobrevive a refatorações?\n- [ ] Nome claro?\n- [ ] Testo MEU código, não framework?\n- [ ] Usei `isVisible()` para visualização?\n- [ ] Testei pela interface quando possível?\n- [ ] Usei `.toBe(true/false)` para booleanos?\n\n**Se NÃO, teste inválido.**\n\n🚫 **Nunca crie testes para inflar cobertura.**\n\n**EXECUTE PASSO 1 AGORA!**"
  },
  "workspaceFolderName": "frontend",
  "shortName": "unit-test-checker"
}