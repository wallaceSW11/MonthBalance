# Environment Variables
# PRODUÇÃO (Docker/AWS): Use /api (proxy reverso do nginx para backend:5150)
# DESENVOLVIMENTO LOCAL: Use http://localhost:5150/api
VITE_API_BASE_URL=/api

# Docker (local only)
PORT=8085
